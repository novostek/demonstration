<div class="content-wrapper-before woffice-color"></div>
<div class="breadcrumbs-light pb-0 pt-4" id="breadcrumbs-wrapper">
  <!-- Search for small screen-->
  <div class="container">
    <div class="row">
      <div class="col s10 m6 l6">
        <h5 class="breadcrumbs-title mt-0 mb-0"><span><%= t('balance') %></span></h5>
        <ol class="breadcrumbs mb-0">
          <li class="breadcrumb-item"><a href="/"><%= t('home') %></a>
          </li>
          <li class="breadcrumb-item"><a href="<%= accounts_path %>"><%= t('accounts') %></a>
          </li>
          <li class="breadcrumb-item active"><%= t('balance') %>
          </li>
        </ol>
      </div>
<!--      <div class="col s2 m6 l6"><a class="btn waves-effect waves-light breadcrumbs-btn right border-round" href="<%#= new_account_path %>"><i class="material-icons hide-on-med-and-up">add</i><span class="hide-on-small-only"><%#= t "add"  %> <%#= t('activerecord.models.account') %></span></a>-->
<!--      </div>-->
    </div>
  </div>
</div>

<%#= form_with url: balance_accounts_path, method: :get do%>
<!--  <div class="form-group">-->
    <%#= label_tag :date, nil, class: 'sr-only'%>
    <%#= date_field_tag :date, @to_date, :class => 'datepicker form-control' %>
<!--  </div>-->
<!--  <button type="submit" class="btn btn-default">Get Report</button>-->
<%# end %>
<div class="card-panel">
<%= render 'account', :name => t('texts.account.assets'), :accounts => @assets %>
<%= render 'account', :name => t('texts.account.liabilities'), :accounts => @liabilities %>
<%= render 'account', :name => t('texts.account.equity'), :accounts => @equity %>
</div>